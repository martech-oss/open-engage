{
  "name": "@openengage/agent",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "test": "vitest run --config vitest.config.ts",
    "deploy": "vite build && wrangler deploy",
    "deploy:bootstrap": "CLOUDFLARE_ENV=bootstrap vite build && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check:types": "pnpm typecheck",
    "cf:types": "wrangler types worker-configuration.d.ts --env-interface AgentBindings --config wrangler.jsonc"
  },
  "dependencies": {
    "@cloudflare/playwright": "^1.3.5",
    "@flue/runtime": "^2.0.1",
    "@openengage/core": "workspace:*",
    "hono": "^4.7.0",
    "just-bash": "^3.2.0",
    "valibot": "^1.4.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.39.2",
    "@flue/cli": "^2.0.1",
    "@flue/vite": "^2.0.1",
    "@types/node": "^22.10.10",
    "typescript": "catalog:",
    "vite": "^8.0.14",
    "vitest": "catalog:",
    "wrangler": "^4.97.0"
  }
}
