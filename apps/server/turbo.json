{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["$TURBO_EXTENDS$", "cf:types"]
    },
    "typecheck": {
      "dependsOn": ["$TURBO_EXTENDS$", "cf:types"]
    }
  }
}
